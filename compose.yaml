services:
  web:
    build:
      context: .
      target: development
    ports:
      - "8080:8080"
    volumes:
      - ./src:/app/src:ro
      - ./server.mjs:/app/server.mjs:ro
      - site-data:/app/data

volumes:
  site-data:
